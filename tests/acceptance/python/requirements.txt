# 验收里的「独立核对」环节用 Python 侧的另一套 PDF 实现交叉验证用例结论，
# 与被测的 pdf.js / pdf-lib 无共享代码，因此能发现实现自身的一致性盲区。
#
#   python3 -m pip install -r tests/acceptance/python/requirements.txt
#
# 装在哪个解释器里，就用 PDFINK_PYTHON 指给验收脚本：
#   PDFINK_PYTHON=/path/to/python node tests/acceptance/acceptance.mjs
#
# 用途：
#   pdf_probe.py     提取输出 PDF 的文字、链接注解与图片对象（A9 段核对）
#   render_pdfium.py 用 PDFium 把页面渲染成位图（A5 / A9 的像素级核对）
#   render_pages.py  按页导出位图，供人工比对
pypdf>=5.0
pypdfium2>=4.30
